# Memory Log - February 10, 2026

## Retail Estimate Module - COMPLETED ✅
**Commit:** 308ca7e "feat: add Retail Estimate module with internal/customer view toggle"

### Features Added:
- Complete retail estimate calculator with material/labor markup system
- Internal view: Shows cost, markup %, markup $, total per item
- Customer view: Clean display without per-item pricing (pricing only in totals)
- Cascading fee structure (fees apply to subtotal + previous fees)
- Default markups: 35% materials, 25% labor
- Customer/Internal view toggle
- PDF generation for both views
- Refresh button to sync from Materials/Labor tabs

### Key Functions Created:
- `initializeRetailEstimate()` - Pulls from materials/labor data
- `calculateRetailTotals()` - Computes subtotal, fees, tax, grand total
- `toggleRetailView(mode)` - Switch between 'internal'/'customer'
- `displayRetailEstimate()` - Main UI render
- `buildRetailPDF()` - PDF generation
- `refreshRetailFromSource()` - Reload items

## Current Session - Customer View Cleanup (IN PROGRESS)

### Three Fixes Being Applied:
1. **Fix 1:** Remove per-item pricing column from customer view HTML table (line ~171)
2. **Fix 2:** Right-align quantity numbers and widen markup input in internal view (lines 180-185)
3. **Fix 3:** Remove per-item pricing from customer view PDF in `buildRetailPDF()`

### Current Code Issue:
Customer view table at line ~171 still shows pricing:
```javascript
return `<tr><td>${item.description}</td><td style="text-align:right;">$${itemTotal.toFixed(2)}</td></tr>`;
```

Should be changed to:
```javascript
return `<tr><td>${item.description}</td></tr>`;
```

### Status: Paused mid-fix for memory compaction

---

## Earlier Today (Feb 10, 2026 - PM Session)

### Completed Features:
- ✅ Labor row individual delete buttons working
- ✅ Address extraction for old Ridge Top PDFs (without zip codes)
- ✅ Made address field editable
- ✅ Zero-quantity items filtered from PDFs
- ✅ Photos clear on new project/PDF upload
- ✅ "Charcoal Black" added to shingle colors

### Commits Made:
- b9c910c - "fix: correct function name in deleteLaborItem" (4:33 PM)
- 9a7285b - "feat: make address editable, add Charcoal Black shingle color" (3:23 PM)
- fd55ed4 - "fix: add fallback address pattern for old Ridge Top PDFs without zip" (3:13 PM)
- Multiple earlier commits for labor delete, photo clearing, etc.

---

*Session paused for compaction - resume with 3 customer view fixes*
